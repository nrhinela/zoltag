Canonical shared UI utilities live here.

Use these modules when you need common rendering or interaction helpers (pagination, selection,
image grids, hotspots, etc.). Legacy files may still re-export from these modules during
migration.
