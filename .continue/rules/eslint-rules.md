---
globs: "**/*.js"
description: This rule ensures consistent coding standards across the project by
  adhering to ESLint configurations specified in .eslintrc.json.
alwaysApply: true
---

Always follow the rules defined in .eslintrc.json, including 2-space indentation, using camelCase for function names, avoiding trailing whitespace, preferring const over let unless reassignment is required.